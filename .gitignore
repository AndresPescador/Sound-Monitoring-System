# ── Variables de entorno reales (nunca subir) ───────────────────────────────
auth-service/.env
ingestion-api/.env
noise-processing-backend/.env
docker/.env
send_metrics/.env
dashboard-frontend/.env
dashboard-api/.env

# ── Artefactos de build y cache ──────────────────────────────────────────────
**/target/
**/__pycache__/
*.pyc
*.jar
*.class

# ── Backups de base de datos (contienen datos reales) ───────────────────────
sql/backups/

# ── Dependencias y build del frontend ────────────────────────────────────────
dashboard-frontend/node_modules/
dashboard-frontend/dist/

# ── Datos de ejecución de send_metrics (tokens, logs, estado) ───────────────
send_metrics/token.json
send_metrics/send_metrics.log
send_metrics/failed_files.json
send_metrics/register_station.py
send_metrics/audio_stats/

# ── Datos de prueba local ────────────────────────────────────────────────────
/api-testing/test_data
